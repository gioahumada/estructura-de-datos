#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#define MAX 1000

struct compra
{
    char *nombreCliente;
    int montoCompra;
    char *fecha;
};

struct compra **compras;

int contarComprasMismaFecha(struct compra **compras; int max; char *fecha ){
    int cont = 0, i;

    for(i=0;i<max;i++)
    {
        if(compras[i]!= NULL)
        {
            srtcmp(compras->fecha[i],decha)
        }
    }
}


